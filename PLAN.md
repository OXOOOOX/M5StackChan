# 开发计划与验收状态

更新：2026-09-03。依据现有代码、远端分支及 2026-05 聊天复核；本次没有操作硬件。

## 阶段状态

| 阶段 | 已有内容 | 结论 |
| --- | --- | --- |
| v0.1.0 监听 | 监听、倒计时、触摸退出，后续补长度/范围过滤 | 历史实机成功；无正式 tag |
| v0.2.0 驱动探索 | VM_EN、UART、驱动/验证 UI、总线诊断、ID 恢复、校准 | 供电、双 ID、Jog、校准有证据；旧驱动仍有问题 |
| v0.3.0 遥控探索 | 自包含脚本、报文过滤、yaw 增量映射、pitch 映射 | 实现存在，连续旋转/回绕与异常恢复未验收 |
| v0.4.0 表情 | Avatar、触摸/IMU 触发、表情与遥控联动 | 未实现，后置 |
| v0.5.0 整合 | 启动菜单、动作编排、设置保存、LED | 未实现，后置 |

## 下一步先完成可靠性验收

- [ ] 修复非 8 字节杂包持续到达时跳过空闲超时检查的问题。
- [ ] 原型先补齐诊断脚本已有的目标 ID、长度边界与校验和检查；两者都需继续核对错误码和 Ping 专用长度。
- [ ] 对 Start/home/Stop 中每个轴的可用状态分别约束；记录实际断电失败，不能仅以异常被捕获判断成功。
- [ ] 统一自包含脚本与 `lib/` 的职责及校准参数；旧驱动 ±180° 换算可能超出 raw 范围，先修复再推荐使用。
- [ ] 重新验证 yaw 方向、raw 0/1023 边界、持续转动和机械行程。取模/最短角差只描述软件目标，不能保证实际电机路径。
- [ ] 从最小幅度验证 pitch 中心、方向与机械限位，记录本机校准值。
- [ ] 逐项记录 Start、Stop、Exit、失联、杂包、单轴缺失的实机行为和供电状态。
- [ ] 固定设备型号、UIFlow2 构建版本、发送端固件 SHA256、接收脚本 Git 提交及测试日志。

验收前继续使用报文监听与独立诊断。证据见 [排错总集](docs/debugging_lessons_zh.md)，实现差异见 [API 文档](docs/servo_api.md)。

## 已确认的初始化基础

| 项目 | 参考值与范围 |
| --- | --- |
| PY32 | I2C `0x6F`，SDA=`G12`，SCL=`G11`，源码使用 pin `0` 控制 VM_EN |
| 舵机串口 | UART1，TX=`G6`，RX=`G7`，`1000000` baud |
| 逻辑 ID | yaw=`1`，pitch=`2`；物理插口不自动分配 ID |
| 中心值 | 官方固定源码为 `460/620`，当前遥控脚本为 `510/610`；均不能替代逐台校准 |
| 字节序 | ESP-NOW `<BhhhB` 小端；SCSCL 的 16 位参数高字节在前 |

## 后续功能

可靠性验收后再考虑表情渲染、眨眼、触摸/IMU 交互、遥控与表情组合、动作关键帧、设置保存和 LED。当前 `laser` 字段仅接收/展示。

历史计划因所参考的小智实现依赖 ESP-IDF 而暂停移植。这是当前实现路线的取舍，不应写成“MicroPython 无法实现任何 AI 功能”。

## 发布顺序

1. 完成 [上传清单](docs/github_upload_review.md) 中的文件整理与静态检查。
2. 提交接收端工作及排错文档，使用开发中状态描述。
3. 合并遥控器分支时人工整合双方 README，核对其固件替换内容。
4. 完成实机验收后再决定正式版本与 tag。
